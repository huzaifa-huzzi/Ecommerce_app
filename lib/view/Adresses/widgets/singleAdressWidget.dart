import 'package:flutter/material.dart';


class SingleAdressesWidget extends StatelessWidget {
  const SingleAdressesWidget({super.key});

  final bool  selectedAdress

  @override
  Widget build(BuildContext context) {
    return
  }
}
